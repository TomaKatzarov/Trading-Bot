"""Global pytest configuration for the Trading Bot project.

Currently no custom hooks are required because runtime patches are handled via
``sitecustomize.py``. This placeholder file keeps pytest happy and documents the
intent.
"""
